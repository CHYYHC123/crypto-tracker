- 逻辑黑盒化 (Black-Box Logic)
  - 现象：由于大量代码由 Cursor 生成，你虽然知道“有什么功能”，但不知道“具体的实现细节”。
  - 结果：二次查看代码时像是在看别人的项目，维护成本极高。
- 逻辑碎片化与嵌套 (Logic Fragmentation)
  - 现象：比如浏览器插件项目中(扩展天生具有 Popup、Background、Content 三端隔离的特性)，nextjs 项目中要不同组件之间、api、服务端组件、客户端组件
  - 结果：AI 为了图快，一般都会把代码写到最近的组件中（逻辑都写在一起），
- 调试定位难 (Debugging Blindness)
  - 现象：当出现 Bug 时，无法快速判断问题出在哪个环节（是 UI 发错了？后台没收到？还是网页脚本没执行？）。
  - 结果：必须从头捋一遍代码，浪费大量时间。

我想的解决方法：设计驱动开发 (Design-Driven Development)

- 核心思路是：在“写代码”之前，增加一层“逻辑审计”流程。

- 针对已存在的逻辑要根据描述，新增